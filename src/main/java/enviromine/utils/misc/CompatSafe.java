package enviromine.utils.misc;

public @interface CompatSafe {
	
	//Annotates classes that used to be `CompatDanger`, but now have separated compatibility logic
	
	//? (When searching) Indicates base classes that have compatibility
}
